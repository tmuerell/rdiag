on: [push]

name: CI

jobs:
  build_and_test:
    name: Rust project
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.build-normal.outputs }}
      output2: ${{ steps.build-musl.outputs }}
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - id: build-normal
        uses: actions-rs/cargo@v1
        with:
          command: build
          args: --release --all-features
      - id: build-musl
        uses: actions-rs/cargo@v1
        with:
          use-cross: true
          command: build
          args: --target x86_64-unknown-linux-musl
  build_windows:
    name: Windows build
    runs-on: windows-2019
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
      - uses: actions-rs/cargo@v1
        with:
          command: build
          args: --target x86_64-pc-windows-msvc